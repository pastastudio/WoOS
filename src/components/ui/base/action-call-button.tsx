import { Button } from '@/components/ui/button';

function ActionCallButton() {
  return (
    <Button id='action-call-button' className='sticky z-50'>
      Save the World!
    </Button>
  );
}

export { ActionCallButton };
